# Debug only. Copy to .env and flutter run.
# Release / GitHub zips always use https://api.nukemarketing.in
#
# Leave empty (or omit this file) for live API.
# NUKE_BASE_URL=http://localhost:8080

NUKE_BASE_URL=
